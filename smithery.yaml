project:
  name: oa-mcp-server
  runtime: typescript  # Smithery uses "typescript" for Node.js/JS projects
  version: 1.0.0

build:
  install:
    - npm install

startCommand:
  command: node server.js
  args: []
